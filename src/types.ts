// TYPE INFERENCING (ts automatically inferences the basic data types)

let drink = "matcha";
let cups = Math.random() > 0.5 ? 10 : '5';

//TYPE ANNOTATION (when we define explicitly like number, boolean, string etc.)
let attendance : string = "full";

let isKid: boolean = true;